# Topten Simple Theme Options — contributor rules

## Regression safety (do not break other features)

**Standing user policy:** plugin or sample changes must not break unrelated Theme Settings flows, theme integrations, or storefront behavior.

- **Scope** — Prefer body-class / screen-specific CSS and context checks in PHP; avoid global selector or hook renames without grep and a migration note.
- **Verify** after substantive edits: Theme Settings save + section nav, Tools → Simple Settings export/import, one theme menu (e.g. UAEBattery), packaged demo samples when demo is on.
- **`php -l`** on every edited PHP file.
- Workspace: **`.cursor/rules/regression-safety.mdc`** (always applied in this Local install).

## Deploy to GitHub (mandatory after every change)

- Work inside **`wp-content/plugins/battery-simple-theme-options/`** (this fork’s git repo; do not use the wordpress.org **`simple-theme-options`** folder name).
- **WordPress.org zip:** build from plugin root respecting **`.distignore`** (excludes **`.cursor`**, **`tools/`**, **`includes/FreemiusBootstrap.php`**, **`vendor/freemius`**, dev markdown). Run Plugin Check against that zip, not the full dev tree, before review.
- **Standing policy:** any substantive plugin change (PHP, JS, CSS, assets, README, rules, sample fields) is **not done** until it is on **`origin/main`**. Always **`git commit`** then **`git push origin main`** in the same session — including doc-only and sample-copy tweaks.
- Ritual after each change set:
  - `git status` and inspect `git diff`
  - `git add …` intentional paths  
  - `git commit -m "$(cat <<'EOF'…EOF)"` — imperative subject (~≤72 chars)
  - **`git push origin main`**
  - Confirm clean tree (`git status`) and report the commit SHA to the user when they care about deploy status.

## Canonical remote

- **`origin`:** **`git@github.com:hsbeauty39/simple-theme-options.git`**  
  Web: **https://github.com/hsbeauty39/simple-theme-options**  

Use **`git remote -v`** after a fork/changed remote.

Push requires a GitHub identity with **push** rights (maintainer **`hsbeauty39`** or a collaborator).

- **2026-05-19 — Theme Settings Customizer:** Single embedded panel under **Theme Settings** panel (`STO_Embed_Control` + `render_customizer_embed()`) — same sidebar/section/field UX as wp-admin; SPA nav in Customizer via **`stoIsCustomizerEmbed()`** in **`main.js`**; save on **Publish** via **`sto_save_theme_options_customizer`**. Fixed **`SimpleThemeOptions\Assets`** import in **`ThemeSettingsCustomizer`**. **`sto-customizer.js`** inits gradient/color/background on embed expand; **`fixStoCustomizerEmbedLayout()`** flattens legacy **`sto-option-panel-nav-layout`** / body shells, enforces **search → sidebar → fields** flex order, and measures/collapses any extra band above the section nav. Gradient stop dock uses **z-index 500110** in Customizer (above **`.wp-full-overlay`** at 500000). Customizer embed markup omits **`sto-option-panel-body`** / nav-layout wrappers; **`sto-customizer.css`** + **`style.css`** stack the pane with flex **`order`** on **`.sto-customizer-embed-root`** (head **1**, sidebar **2**, main **3**) so the main column cannot reserve ~500px above the nav when only the sidebar had a positive **`order`** before.
- **2026-05-19 — WC Product data: no editors in repeaters:** **`rich_modern_editor` / `RichModernEditor` removed** (field class, assets, samples). **`advanced_repeater`** on menus with **`woocommerce_product_data`** must not use **`editor`** leaves — admin shows a warning; values are stripped on save. Use **`textarea`** or standalone **`editor`** fields outside the repeater. **`_doing_it_wrong`** on register when schema includes **`editor`**.
- **2026-05-19 — Advanced repeater classic editor (Visual live mirror):** **`sto-advanced-repeater-editor.js`** binds TinyMCE change handlers on existing + newly booted instances; **`shouldKeepTextareaOverTinyMce()`** no longer blocks live sync when Visual has focus or when the user types short HTML (only blocks empty TinyMCE vs long Code-tab source).
- **2026-05-19 — Advanced repeater table detection:** **`leafHasTableMarkup()`** requires a closed **`<table …>`** opener (partial **`<table`** while typing does not force Code mode or show the table notice).
- **2026-05-19 — Repeater Visual + Add Media save:** **`stoFlushRepeaterVisualEditorToTextarea()`** copies **`tinyMCE.getContent()`** into the textarea when the canvas has **`<img>`** / media but **`save()`** would leave the textarea empty; **`readClassicEditorHtmlFromLeaf()`** prefers Visual HTML in that case; PHP **`merge_repeater_row_leaves()`** picks leaf HTML with **`classic_editor_html_has_meaningful_content()`**.
- **2026-05-19 — Advanced repeater WC save:** **`stoEnsureWcRepeaterPostLevelHiddens()`** + **`stoBridgeWcProductDataFieldsToPostForm()`**; metabox **`sto_metabox.active`** off on WC product data; inject leaf-row fallback.
- **2026-05-19 — Advanced repeater WC save (editor rows):** **`Input::classic_editor_html_has_meaningful_content()`** keeps img/media-only editor rows; **`sto-advanced-repeater-field.js`** merges DOM + leaf POST per row index before **`sto_options[id]`** JSON; inject always updates posted JSON.
- **2026-05-18 — Export backup JSON:** `ajax_export` boots sample field modules + `FieldRegistrationDeferral::flush()` so the registry is populated on `admin-ajax.php`. Tools **Export scope** requires ≥1 checked menu; JS shows server error text instead of generic “Could not create the export” when possible.
- **2026-05-18 — Plugin Check / text domain:** Plugin slug / **Text Domain** = **`topten-simple-theme-options`**. **`STO_TEXT_DOMAIN`** constant matches it (used for `load_plugin_textdomain()` only). **`__()` / `_n()` / `esc_html__()`** must pass the **literal** string `'topten-simple-theme-options'` (Plugin Check rejects a constant as the domain argument). Recommended install **folder** remains **`battery-simple-theme-options`** (`STO_PLUGIN_SLUG`). **PHPCS ruleset:** `docs/phpcs.xml` (not in org zip; avoids Plugin Check `application_detected` on `*.dist`). **Cursor rules:** workspace `.cursor/rules/topten-simple-theme-options-plugin.mdc` (plugin `.cursor/` removed so Plugin Check does not flag `ai_instruction_directory`). Run Plugin Check on a **distribution zip** before review; `.gitignore` / `.distignore` may still warn on a full dev tree scan.
- **2026-05-18 — Theme Settings SPA nav:** `main.js` resolves links with **`stoAbsAdminHref()`** against **`trailingslashit( admin_url() )`** (trailing slash required — without it, `new URL('admin.php', base)` becomes `/admin.php` at site root). **`pushState`** only on the main settings screen under **`/wp-admin/`**; from Freemius Contact/Pricing, **`#adminmenu`** uses **`window.location.href`**. Broken URLs auto-redirect via **`stoRecoverBrokenThemeSettingsUrl()`**.
- **2026-05-18 — Developer guide location:** HTML integrator bundle moved from plugin root to **`wp-content/plugins/instructions/`** (`instructions.html` + `scripts/`). Not shipped in plugin ZIP; deploy separately for live docs URL.
- **2026-05-17 — Tools screen label:** **Tools → Simple Settings** (`sto-simple-backup`); first sidebar section **Settings** (`section=backup`), not “Simple Backup” / “Backup”.
- **2026-05-17 — Premium panel banner:** When Pro is off, **`PremiumFieldGate::render_panel_banner()`** outputs a full-width upgrade alert + **Purchase Premium** CTA above the panel head on Theme Settings root screens; filter **`sto_show_premium_panel_banner`**.
- **2026-05-17 — Demo Groups & panels section:** Packaged **Theme Settings** menu adds leaf **`groups-panels`** (`?section=groups-panels`) — **Group**, **Advanced repeater**, and **Accordion** demos in `includes/Admin/Sample/Fields/GroupsPanels/GroupsPanels.php` (replaces standalone **Accordion** nav).
- **2026-05-17 — Demo Responsive section:** Packaged **Theme Settings** menu adds leaf **`responsive`** (`?section=responsive`) with all `'responsive' => true` sample fields moved from **Field samples** / **Accordion** into `includes/Admin/Sample/Fields/Responsive/Responsive.php`.
- **2026-05-17 — Freemius Upgrade styling:** Non-Pro sites load **`sto-freemius-upgrade.css`** (admin-wide) for gradient **Upgrade** / **Start trial** WP submenu rows; Theme Settings sidebars append **`PremiumFieldGate::render_sidebar_upgrade_cta()`** (gradient **Upgrade to Pro** → Freemius **`pricing_url()`**; **`main.js`** skips SPA `pushState` for that link). Locked field CTAs in **`sto-premium-locked.css`** use the same horizontal purple→orange gradient.
- **2026-05-17 — Freemius Contact Us:** `StoFreemiusContact` filters `templates/contact.php` — STO hub + topic cards open the standalone Freemius form (no wp-admin iframe). CSS: **`sto-freemius-contact.css`**.
- **2026-05-17 — Freemius Plans & Pricing:** `StoFreemiusPricing` filters `templates/pricing.php` — STO panel header + purple design tokens on `#fs_pricing_app`; compact gradient **Upgrade** CTA. CSS: **`sto-freemius-pricing.css`**; body class **`sto-fs-pricing-screen`**.
- **2026-05-17 — Gradient dock color picker:** Dock portaled to **`document.body`**; **`click.stoGradOutside`** ignores the open dock. Iris **`.iris-picker-inner { position: relative }`** (in-flow, same as palette UI) so square/strips do not overlap **`.sto-gradient-dock-palette`**. **`sto-color.css`** does not style the dock.
- **2026-05-17 — Packaged demo + Freemius:** **Theme Settings** top-level menu **always** registers; demo mode only toggles sample nav/fields. Freemius defaults to **`theme-settings`** (`sto_freemius_menu_slug` filter optional).
- **2026-05-19 — Rich modern editor:** New premium field type **`rich_modern_editor`** (`RichModernEditor`) — Gutenberg block editor in Theme Settings / groups / tabs / accordions; stores serialized blocks; assets **`sto-rich-modern-editor`**; boot **`RichModernEditor::instance()`** before register. **Integrator docs:** **`wp-content/plugins/instructions/instructions.html`** § **6.14a**, PHP sample **`#php-rich_modern_editor`**.
- **2026-05-19 — Rich modern editor in advanced repeater:** **`advanced_repeater`** leaves may use **`rich_modern_editor`**; mount on row expand/add. **`sto-rich-modern-editor.js`** uses **`BlockList`** only (no extra **`DefaultBlockAppender`**). WC product-data + adv-rep CSS keep inputs/editor inside repeater cards (`min-width: 0`, `max-width: 100%`). **`.sto-rich-modern-editor__input`** stays screen-reader hidden in repeater rows (exclude from **`.sto-adv-rep__input`** width rules). Image library/upload: register **`editor.MediaUpload`** → **`wp.mediaUtils.MediaUpload`**, set **`settings.mediaUpload`**, deps **`wp-media-utils`** + **`wp_enqueue_media()`**.
- **2026-05-19 — WC product data save:** **`stoPrepareWcProductDataPanelsForSubmit()`** in **`main.js`** syncs advanced repeater JSON + enables fieldsets before **`#post`** submit; values persist to **`_sto_theme_settings_post`**. **`AdvancedRepeaterControl`** drops empty rows on save and skips them for **`html_required`** (avoids blank placeholder rows blocking Product Tabs save).
- **2026-05-19 — Rich modern editor after tables:** **`BlockList`** uses **`DefaultBlockAppender`** when empty, **`ButtonBlockAppender`** when blocks exist (avoids extra empty paragraph “noise”); **`BlockTools`** when available; **`stoFlushRichModernEditorsToInputs()`** before repeater sync on WC product save; PHP **`markup_has_meaningful_content()`** + empty **`core/paragraph`** pruning on sanitize; adv-rep empty-row detection uses the same helper so phantom editor markup does not block Product Tabs save. WC validation errors surface via **`admin_notices`** (not **`wc_add_notice`** in admin).
- **2026-05-19 — WC product save via Enter:** **`#post`** capture-phase submit + Enter/Cmd+S prep (flush editors, sync repeaters) before implicit submit; hidden **`sto_wc_return_section`** / **`sto_wc_active_panel`** + **`redirect_post_location`** restore STO section and Product data tab after save; sidebar/quick-search Enter no longer hijacks navigation.
- **2026-05-19 — Product Tabs custom repeater save:** **`main.js`** keeps **`.sto-adv-rep__value`** and WC panel fieldsets enabled on submit; **`stoRunWcProductDataPreSubmit()`** / **`stoPrepareWcProductDataPanelsForSubmit()`** call **`stoSaveRepeaterTinyMceEditors()`** then **`stoSyncAdvancedRepeaterFields()`** on the full **`#post`** / WC panel scope. **`sto-advanced-repeater-field.js`** reindexes leaf **`name`** attributes before sync, uses **`readLeaf()`** (not raw **`.val()`**) for classic **`editor`** leaves in **`collectRootFromLeafPostNames()`**, and mirrors editor HTML into the textarea before POST. **`AdvancedRepeaterControl::parse_items()`** tolerates slashed JSON; **`walk_html_required`** / sibling checks treat **`editor`** markup like storage empty checks. Battery BD removed **`html_required`** on **`tab_name`** (rows without names are dropped on sanitize).
- **2026-05-19 — Advanced repeater fieldset rows (Battery Specifications):** Repeater schemas with nested **`fieldset`** (e.g. **`spec_row` → spec_name / spec_value / spec_display_type**) failed on WC product save because **`collectRootFromLeafPostNames()`** posted flat leaf keys and **`pickRicherRepeaterRows()`** treated nested JSON as empty. **`collectRootFromLeafPostNames()`** now uses **`collectItemBody()`**; **`rowObjectHasMeaningfulContent()`** recurses into nested objects; PHP **`normalize_adv_rep_row_for_schema()`** + **`merge_repeater_row_leaves()`** fieldset merge nest flat POST under the fieldset id before sanitize.
- **2026-05-19 — Rich editor flush (repeater save):** **`stoFlushRichModernEditorsToInputs()`** uses a **`blocksRef`** so pre-submit flush does not overwrite the hidden textarea with stale React state; flush triggers **`change`** on the textarea; **`syncFromAny`** flushes rich editors inside the repeater before JSON sync. **`sto-premium-locked`** CSS no longer depends on **`sto-freemius-upgrade`** when Pro is active (fixes WP 6.9.1 style notice on product edit).
- **2026-05-19 — WC advanced repeater leaf POST backup:** On **`woocommerce_product_data_panels`**, repeater leaves also submit as **`sto_options_adv_rep_leaves[field_id][row][leaf]`** (Tab name + block markup textarea). **`AdvancedRepeaterControl::merge_repeater_row_sets()`** merges per leaf (non-empty **`tab_content`** from POST wins over empty hidden JSON). **`Menu::persist_all_woocommerce_product_data_panels_from_request()`** saves all WC STO menus on product save (priority **15**). **`sto-rich-modern-editor.js`**: sync blocks → textarea on every blocks change + flush before repeater JSON sync; save uses full **`serialize()`** (no pre-save prune). JS reindexes leaf **`name`** on add/sort.
- **2026-05-19 — WC Product Tabs block editor click/mount:** **`AdvancedRepeaterControl`** primes core block editor packages when any schema includes **`rich_modern_editor`** (Product Tabs has no standalone RichModernEditor field). **`sto-rich-modern-editor.js`**: do not unmount on defer; mount when WC panel/repeater body is visible; **`useSubRegistry: false`**; sync textarea in **`onInput`** only; stop click propagation on the canvas. **`sto-wc-product-data-panels.css`**: no **`pointer-events: none`** on WC fieldsets. **`display-section-on-menu`** depends on **`sto-rich-modern-editor`** (one-way only — never add **`display-section-on-menu`** as a dep of **`sto-rich-modern-editor`** or WP exhausts memory resolving the cycle).
- **2026-05-19 — Advanced repeater `editor` leaf:** Classic **`wp_editor()`** in repeater rows; **`wp_kses_post`** after **`repair_editor_rn_corruption()`**. Never **`wp_unslash()`** editor HTML after **`json_decode`** / leaf bucket (second unslash turns `\r\n` into literal **`rn`**). Leaf bucket is unslashed once at bucket read. Repair runs on render, save, and JS sync (**`stoRepairRepeaterEditorRn`**). **Add item:** reindex row **`id` / `name`** (including **`wp-{id}-wrap`** / **`qt_{id}_toolbar`**) before **`clearItem()`**; **`stoStripRepeaterClassicEditorLeaves`** + **`wp.editor.initialize`** on a bare textarea (description above editor). **`sto-advanced-repeater-field.css`** + **`sto-wc-product-data-panels.css`**: repeater classic editors use **block layout** (static **`.wp-editor-tools`**, full-width **`.mce-tinymce`**) — not the Theme Settings toolbar overlay from **`sto-input.css`**.
- **2026-05-19 — Repeater editor Visual/Code + tables:** **`Input::normalize_editor_html_for_visual()`** wraps bare **`<tr>text</tr>`** in **`<td>`** before TinyMCE Visual (invalid table rows otherwise flatten to concatenated plain text). **`sto-advanced-repeater-editor.js`**: capture-phase tab guard blocks Visual when **`<table>`** is present (TinyMCE has no core table plugin — Visual strips markup); **`switchEditors.go(id,'html')`** + **`repairRepeaterEditorWrapState()`** instead of synthetic tab clicks; mousedown backup restores textarea HTML; Code tab always reads **`stoReadClassicEditorHtmlFromLeaf()`** before switch. Repeater editors use relaxed TinyMCE (**`verify_html`/`cleanup` off**, **`wpautop` false**).
- **2026-05-19 — Classic editor UI parity:** Shared class **`sto-classic-editor-field`** on Theme Settings **`Input`** rows and advanced-repeater **`editor`** leaves. **`sto-input.css`** toolbar overlay (Add Media + Visual/Code on the first TinyMCE row) applies to **standalone** Theme Settings fields only; **repeater** **`editor`** leaves override to stacked block layout (WC Product data + adv-rep CSS).
- **2026-05-19 — Classic editor tables / rich HTML:** **`Input::get_classic_editor_allowed_html()`** (post tags + **`class`** / **`id`** on tables, **`rowspan`**, **`colspan`**, etc.). Save uses that allowlist; **`unfiltered_html`** cap keeps raw HTML. **Never** call global **`tinymce.triggerSave()`** — use **`stoSaveVisibleTinyMceEditors()`** / **`stoReadClassicEditorHtmlFromLeaf()`** so Code-tab / table markup is not wiped. **Do not** add TinyMCE **`table`** (not in WP core — breaks editor). Repeater **`<table>`** opens in **Code** mode; Visual click with tables stays on Code. Storefront: **`wp_kses`** + allowlist (not **`the_content`** / wpautop).
- **2026-05-19 — WC Product Tabs repeater save:** Leaf POST backup **`sto_options_adv_rep_leaves`** + **`inject_repeater_leaf_bucket_into_sto_options_post()`** merge before Menu save.
- **2026-05-19 — Repeater Visual Add Media wipe fix:** **`pickRichestRepeaterEditorHtml()`** keeps the longest meaningful HTML from textarea mirror / TinyMCE / backup (never let **`tinyMCE.save()`** clear media on submit); Visual **`input`→TinyMCE** mirror Code-only; WC bridge + **`removeAttr('name')`** on editor textarea; PHP **`pick_richest_editor_html_for_merge()`**; single pre-submit flush path in **`stoRunWcProductDataPreSubmit()`**.
- **2026-05-19 — Rich modern editor typing:** Do not **`pruneEmptyBlocks`** on live **`onInput`** (only on flush/serialize); fixes “can’t edit” empty canvas. WC CSS keeps adv-rep block canvas **`pointer-events: auto`** on the active section.
- **2026-05-21 — Install telemetry:** **`InstallationTracker`** reports **`install`** on activation and **`uninstall`** via **`uninstall.php`** to **Topten Track Plugin Installation** (`POST /wp-json/topten-track/v1/event`, header **`X-Topten-Track-Key`**). Blocking POST on lifecycle events; pending option **`sto_installation_tracker_pending`** retries on **`init:20`** when the tracker plugin loads after STO. Same-site auto-wiring via filters; remote sites use **`STO_INSTALLATION_TRACKER_*`** constants in **`wp-config.php`**.
- **2026-05-21 — WooCommerce textdomain (WP 6.7):** Register **`woocommerce_*`** hooks via **`Admin\WooCommerceIntegration`** on **`woocommerce_init`** (not at field boot). Enqueue **`woocommerce_admin_styles`** as a style dependency only when that handle is already registered.
- **2026-05-19 — Instructions bundle policy:** Any new/changed STO field must update **`wp-content/plugins/instructions/`** in the same change set (see workspace **`.cursor/rules/sto-instructions-field-docs.mdc`**).
- **2026-05-16 — Premium fields:** DynamicObject, AdvancedRepeater, GoogleMap, CodeEditor, RichModernEditor, Tabs, Accordion, Group are Pro-only; check with `topten_sto()->can_use_premium_code__premium_only()` / `sto_can_use_premium_fields()`. Locked rows each show the full gradient upsell card (field name + type label + CTA) via **`PremiumFieldGate::render_controls_or_locked_placeholder( $title, $type )`**. Groups render free children when locked; Theme Settings samples use **one root group per section** and standalone premium field registration.
- **2026-05-16 — Typography Select2:** Ship **`assets/admin/vendor/select2/`** (4.0.13); without it **`jQuery.fn.select2`** is missing. Use **`stoRefreshTypographySelect2`** after visibility / fieldset enable (**`sto-typography.js`**, **`main.js`**). **`initTypographySelectsInWrap`** must not destroy/re-init an already-attached control (fixes dropdown flash-close on click); **`refreshTypographySelect2`** skips while **`.select2-container--open`**.
- **2026-05-16 — Theme Settings reset:** Footer **Reset section** / **Reset all fields** (confirm → POST). **`Menu::maybe_handle_reset_request()`** + **`ThemeSettingsDefaults`** restore registration defaults into **`sto_options`**; all-fields redirect drops **`section`** from the URL.
- **2026-05-16 — Sample field copy:** Demo registrations under **`includes/Admin/Sample/`** keep **`description`** to **≤10 words** (long API/theme docs removed).
- **2026-05-16 — Always push:** Every agent/user session that changes this plugin must end with **`git push origin main`** (see **Deploy to GitHub** above). Persisted in **`.cursor/rules/simple-theme-options.mdc`** checklist §12.

## Persist “always do …” instructions (new chats)

When the conversation defines **lasting** plugin policy (integrations, branching, forks, QA, naming, deployments):

1. Update **`.cursor/rules/simple-theme-options.mdc`** checklist **§§12–13** and related sections (+ **README**, `wp-content/plugins/instructions/` when checklist §2 says so — see rule file §2 **Docs** block).
2. Update **this file** (`docs/RULES.md`) with a dated one-line bullet if it helps scanning.
3. **Commit + push** with the implementation or immediately after — **fresh Cursor chats** must read obligations from Git, not memory.

Full binding detail: **`.cursor/rules/simple-theme-options.mdc`** (Mandatory agent checklist, **Architecture**, **Git workflow**).
